---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"a"> {
  noArrow?: boolean;
}

const { href, noArrow, ...attr } = Astro.props;
---

<a target="_blank" rel="noreferrer" {href} {...attr}>
  <slot />
  {!noArrow && <>&nearr;</>}
</a>
